{
  "name": "skill-steward-workspace",
  "private": true,
  "version": "0.0.0",
  "packageManager": "pnpm@10.12.1",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "release:check": "node scripts/release-contract.mjs check",
    "release:sync": "node scripts/release-contract.mjs sync",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "test:preflight-quality": "pnpm --filter @skill-steward/preflight test:quality",
    "test:phase7": "pnpm --filter @skill-steward/store test && pnpm --filter @skill-steward/integrations test && pnpm --filter skill-steward exec vitest run tests/platform-security-workflow.test.mjs tests/native-workflow-policy.test.mjs",
    "typecheck": "pnpm -r typecheck",
    "check": "pnpm release:check && pnpm build && pnpm typecheck && pnpm test"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "esbuild": "^0.25.5",
    "typescript": "^5.8.3",
    "vitest": "^3.2.4",
    "yaml": "^2.9.0"
  }
}
